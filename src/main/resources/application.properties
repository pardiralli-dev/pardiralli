root_url=${ROOT_URL}
root_prefix=${ROOT_PREFIX}

security.basic.enabled=false

spring.datasource.url=${PARDIRALLI_DB_URL}
spring.datasource.username=${PARDIRALLI_DB_USER}
spring.datasource.password=${PARDIRALLI_DB_PASS}
spring.datasource.driver-class-name=org.postgresql.Driver

admin.username=${ADMIN_USERNAME}
admin.password=${ADMIN_PASSWORD}

mail.protocol=smtp
mail.smtp.host=smtp.zoho.eu
mail.smtp.port=465
mail.smtp.socketFactory.port=465
mail.smtp.auth=true
mail.smtp.startssl.enable=true
mail.smtp.debug=true
mail.smtp.socketFactory.fallback=false
mail.username=${PARDIRALLI_MAIL_USER}
mail.from=${PARDIRALLI_MAIL_FROM}
mail.password=${PARDIRALLI_MAIL_PASS}

spring.messages.fallback-to-system-locale=false

sms.twilio.sid=${TWILIO_ACCOUNT_SID}
sms.twilio.token=${TWILIO_AUTH_TOKEN}
sms.twilio.number=+37259120183